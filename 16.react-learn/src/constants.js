const REACT_ELEMENT = Symbol('react.element');
const REACT_TEXT = Symbol('react.text');
const REACT_FORWARD_REF = Symbol('react.forward_ref');
const MOVE = Symbol('move'); //需要移动的节点
const PLACEMENT = Symbol('placement'); //新插入的节点
export {
    REACT_ELEMENT,
    REACT_TEXT,
    REACT_FORWARD_REF,
    MOVE,
    PLACEMENT
}